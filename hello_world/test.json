{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch hello.py",
            "program": "${workspaceFolder}/hello_world/hello.py"
        }
    ]
}